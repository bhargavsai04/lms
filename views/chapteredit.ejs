<!DOCTYPE html>
<html>
  <head>
    <!-- Document Title -->
    <title>Add Chapter Page</title>
    <!-- External Stylesheet for Tailwind CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
    <!-- CSRF Token Meta Tag -->
    <meta name="csrf-token" content="<%= csrfToken %>" />
  </head>

  <body class="bg-gray-100 flex items-center justify-center h-screen">
    <!-- Navigation Links -->
    <div class="absolute top-6 left-6 space-x-4">
      <!-- Home Link -->
      <a href="/show" class="p-4 text-gray-800 bg-white rounded shadow-md"
        >Home<span class="ml-2 text-blue-500">&#8594;</span></a
      >
      <!-- Create Chapter Link -->
      <a href="" class="p-4 text-gray-800 bg-white rounded shadow-md"
        >Create A Chapter<span class="ml-2 text-blue-500">&#8594;</span></a
      >
    </div>
    <!-- Add Chapter Form Container -->
    <div class="bg-white p-8 rounded shadow-md w-1/3">
      <!-- Form Heading -->
      <h1 class="text-2xl text-gray-800 mb-6">Add Chapter</h1>
      <!-- Add Chapter Form -->
      <form action="/chapteredit/<%= notee.id %>" method="post">
        <!-- CSRF Token Hidden Input Field -->
        <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
        <!-- Chapter Title Input Field -->
        <input
          type="text"
          name="title"
          placeholder="Enter chapter name"
          class="w-full p-2 mb-4 rounded border border-gray-300"
          required
        />
        <!-- Chapter Description Textarea -->
        <div class="mt-6">
          <textarea
            name="description"
            class="w-full h-40 p-2 rounded-md border border-gray-300 focus:outline-none focus:ring focus:ring-green-400"
            placeholder="Write something..."
            required
          ></textarea>
        </div>
        <!-- Submit Button -->
        <button
          type="submit"
          class="bg-blue-500 text-white p-2 rounded hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-green-500"
        >
          Submit
        </button>
      </form>
      <!-- End of Add Chapter Form -->
    </div>
  </body>
</html>
